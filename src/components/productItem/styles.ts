import styled from "styled-components";

export const Container = styled.div`
    transition:all ease .5s;
    .area{
        transition:all ease .5s;
    .button{
        transition:all ease .5s;
    }
}
        &:hover .button{
            display:block;
        }

`