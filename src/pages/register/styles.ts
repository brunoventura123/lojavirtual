import styled from "styled-components";
import bg from '../../images/eletronic.jpg'

export const Container = styled.div`
    background:linear-gradient(rgba(0,0,0,0.7),
        rgba(0,0,0,0.7)),
         url(${bg});
    background-position:center;
    background-size:cover;
`